#include <iostream>
#include <vector>
using namespace std;


vector<bool>kids(vector<int>& candies,int extra){
	vector<bool>res(candies.size(),false);
	int max=*maxc(candies.begin(),candies.end());
	for(int i=0;i<candies.size;i++){
		if(max-candies[i]<=extra){
			res[i]=true;
		}
	}
	return res;

}

int main(){
	vector<int> candies
